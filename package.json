{
  "name": "timothycreekmore-com",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for timothycreekmore.com (hub, portfolio, shop, content, game splash).",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/tailwind.src.css -o ./assets/css/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./assets/css/tailwind.src.css -o ./assets/css/tailwind.css --watch",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
